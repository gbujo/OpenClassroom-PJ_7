# setup.py
from setuptools import setup, find_packages

setup(
    name='predict',  # Nom de votre package
    version='0.1.0',  # Version de votre package
    packages=find_packages(),  # Recherche automatiquement les packages
    # ... autres métadonnées (dépendances, auteurs, etc.)
)